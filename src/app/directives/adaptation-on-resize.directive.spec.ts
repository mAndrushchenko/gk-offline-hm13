import { AdaptationOnResizeDirective } from './adaptation-on-resize.directive';

describe('AdaptationOnResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new AdaptationOnResizeDirective();
    expect(directive).toBeTruthy();
  });
});
